(* let a = 3;; *)
let rec f = (lam x:int. x) in f 30;;
(* let rec g = (lam x: int. if x > 0 then true else false) in g 56;; *)
let rec h = (lam x: int. if 1 > x then 0 else x + (h (x + -1))) in h 4;;
(* let rec h = (lam x: int. 0 + h x) in h 5;; *)
